import { useState } from 'react';
export default function AIAssistant({ onClose }: { onClose: ()=>void }){ const [messages,setMessages]=useState([{sender:'ai',text:'Hello 👋, I’m NeuroEdge AI. How can I assist you today?'}]); const [input,setInput]=useState(''); const handleSend=()=>{ if(!input.trim()) return; setMessages([...messages,{sender:'user',text:input},{sender:'ai',text:'I’ll process that locally...'}]); setInput(''); }; return (<div className='fixed right-0 top-0 h-full w-80 bg-white shadow-lg border-l border-gray-200 flex flex-col z-50'><div className='p-4 bg-blue-600 text-white flex justify-between items-center'><h4 className='font-bold'>NeuroEdge AI Assistant</h4><button onClick={onClose}>✖</button></div><div className='flex-1 overflow-y-auto p-4 space-y-2'>{messages.map((m,i)=> <div key={i} className={`p-2 rounded-xl ${m.sender==='ai' ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-800 self-end'}`}>{m.text}</div>)}</div><div className='p-3 border-t flex gap-2'><input className='flex-1 border rounded-xl p-2' placeholder='Ask something...' value={input} onChange={(e)=>setInput(e.target.value)}/><button onClick={handleSend} className='bg-blue-600 text-white px-3 rounded-xl hover:bg-blue-700'>Send</button></div></div>); }
