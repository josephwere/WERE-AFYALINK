import Link from 'next/link'; import { useRouter } from 'next/router';
export default function Sidebar(){ const router = useRouter(); const links = [{href:'/dashboard',label:'Dashboard'},{href:'/patients',label:'Patients'},{href:'/appointments',label:'Appointments'},{href:'/billing',label:'Billing'},{href:'/reports',label:'Reports'},{href:'/analytics',label:'Analytics'},{href:'/settings',label:'Settings'},{href:'/support',label:'Support'}]; return (<div className="w-64 bg-blue-700 text-white min-h-screen p-4 flex flex-col"><h3 className="text-2xl font-bold mb-8">AfyaLink</h3>{links.map(l=> <Link href={l.href} key={l.href}><div className={`p-2 rounded-md mb-2 cursor-pointer hover:bg-blue-500 transition-all ${router.pathname===l.href ? 'bg-blue-500':''}`}>{l.label}</div></Link>)}</div>); }
