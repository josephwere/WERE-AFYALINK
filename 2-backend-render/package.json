{
  "name": "afyalink-backend",
  "version": "1.0.0",
  "description": "AfyaLink — Smart Health System Backend",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "start:local": "npm run build && node dist/server.js",
    "prebuild": "npx prisma generate",
    "migrate:dev": "npx prisma migrate dev",
    "migrate:deploy": "npx prisma migrate deploy",
    "prisma:studio": "npx prisma studio",
    "clean": "rm -rf dist"
  },
  "author": "GoldEdge Labs",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^6.18.0",
    "typescript": "^5.6.3",
    "@types/node": "^20.11.30",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17"
  }
}
