# 🌍 Environment Configuration for AfyaLink Backend

# --- Server Settings ---
PORT=4000
NODE_ENV=development

# --- Database ---
# Replace this with your actual Render DATABASE_URL in local .env
# e.g. postgresql://user:password@hostname:5432/yourdbname
DATABASE_URL=postgresql://user:password@host:5432/database_name

# --- Authentication ---
# Replace with your secure random JWT secret in production
JWT_SECRET=your_super_secret_key_here

# --- CORS ---
# The URL of your frontend (for development or production)
CORS_ORIGIN=http://localhost:3000
